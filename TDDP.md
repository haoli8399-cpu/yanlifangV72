# Trae Design Design Package（TDDP）— 演立方 V4.7

> **这是 Trae Design 的唯一设计事实源。所有设计决策必须基于本文件。**
> 版本：V1.0 | 日期：2026-07-11 | 负责人：Hermes

---

## 1. Product Vision（产品愿景）

### 1.1 产品定位
**AI 提案获客与内容供应链平台。** 用 AI 将企业客户的自然语言活动需求，自动转化为可视化活动方案，连接供需两端。

### 1.2 核心价值
- 客户用一句话描述需求 → AI 自动提取关键要素 → 生成专业方案 → 匹配执行团队
- 供应商获得高质量、结构化的商机线索
- 平台运营获得数据驱动的业务洞察

### 1.3 目标用户
| 角色 | 使用场景 | 核心动机 |
|:---|:---|:---|
| 企业活动决策人 | 年会/团建/发布会前 2-4 周 | 快速出方案、确定预算、对比供应商 |
| 活动公司销售 | 每日处理 5-10 条线索 | 高效转化、减少方案撰写时间 |
| 平台运营 | 周度/月度 review | 数据驱动决策、内容质量管控 |
| C 端消费者 | 偶发活动灵感 | 浏览活动方案、提交需求 |

### 1.4 品牌关键词
**专业 · 可信 · 智能 · 精准 · 克制**

不是娱乐，不是电商，不是工具集合。是面向企业决策的 AI 顾问。

### 1.5 竞争差异
- 不是 AI Chatbot 套壳，而是有完整业务闭环
- 不是信息撮合平台，而是 AI 主动提取需求 + 生成方案
- 不是纯 SaaS 工具，而是增长工具引流 + CRM 转化 + 方案交付的全链路

### 1.6 当前阶段
**MVP 快速验证期。** 核心流程已闭环（工具 H5 → 留资 → CRM → 方案 H5），在优化 UI/UX 质量。

### 1.7 本轮设计目标
- 将 52 个现有页面提升到统一的产品级视觉质量标准
- 建立完整、可交付的 Design System
- 为后续 Lovable/v0/Codex 提供清晰的设计手把（Design Handoff）
- **不改变业务逻辑、不新建路由、不推翻现有技术架构**

---

## 2. UX Strategy（体验策略）

### 2.1 用户为什么来？
- **有明确活动需求但不知从何下手** → 希望有人/工具帮他理清需求
- **需要快速出方案给老板/客户看** → 希望一键生成专业的提案
- **在多个供应商间比价** → 希望有一站式的方案对比

### 2.2 用户为什么留下？
- 方案生成快（几分钟 vs 传统几天）
- 方案质量高（有演员、预算、案例，而不是空泛建议）
- 交互简单（自然语言输入，不需要填表）

### 2.3 用户为什么持续使用？
- 历史方案可回溯、可复用
- 推荐的供应商持续更新
- 每次使用都能获得比上次更好的匹配

### 2.4 核心设计重点
1. **消除"不知道下一步做什么"的迷茫** — 每个页面都有明确的下一步动作
2. **信息不要一股脑塞给用户** — 识别→补全→推荐，分步推进
3. **AI 存在感恰到好处** — 关键决策点出现，不抢用户节奏
4. **B 端销售端的效率感** — 高密度但分层清晰，快速切换商机
5. **客户端的信任感** — 方案页像专业 PDF，不花哨

---

## 3. User Personas（用户画像）

### P1: 企业活动决策人 (Agent 端)
| 维度 | 描述 |
|:---|:---|
| 典型角色 | 行政总监 / HR 经理 / 品牌市场经理 |
| 使用频率 | 每季度 1-2 次，会前 2-4 周集中使用 |
| 关键任务 | 描述需求 → 查看方案 → 比较方案 → 确认意向 |
| 关注点 | 方案是否专业、预算是否合理、执行团队是否靠谱 |
| 操作特点 | 非高频用户，引导要清晰，信息要结构化 |
| 情感需求 | 被理解（需求不用说得太专业）、被重视（方案像专属的）、有掌控感（随时可修改） |

### P2: 活动公司销售 (Supplier 端)
| 维度 | 描述 |
|:---|:---|
| 典型角色 | 销售运营 / 客户经理 |
| 使用频率 | 每日 |
| 关键任务 | 看线索 → 跟进 → 出方案 → 报价 → 追踪状态 |
| 关注点 | 效率（快速切换商机）、信息完整度、AI 辅助 |
| 操作特点 | 高频高密度操作，信息架构要支持快速切换和批量处理 |
| 情感需求 | 省时间、少出错、AI 帮我想好下一步说什么 |

### P3: 平台运营 (Admin 端)
| 维度 | 描述 |
|:---|:---|
| 典型角色 | 运营经理 / 内容审核 |
| 使用频率 | 每日/每周 |
| 关键任务 | 看大盘 → 管理内容 → 审核 → 配置 |
| 关注点 | 数据趋势、内容质量、系统配置 |
| 操作特点 | 后台管理型，信息密度中高，报表和列表为主 |

### P4: C 端消费者 (m 端)
| 维度 | 描述 |
|:---|:---|
| 典型角色 | 活动灵感探索者 |
| 使用频率 | 偶发 |
| 关键任务 | 浏览方案 → 发现灵感 → 提交需求 |
| 关注点 | 方案好不好看、有没有参考价值、提交方便 |
| 操作特点 | 移动端为主，浏览型，低决策压力 |

---

## 4. User Journey（用户旅程）

### Journey A: 增长工具获客流程
```
① 微信端打开工具 H5
   ↓ 为什么存在：免费工具是获客入口，首屏必须建立信任
② 封面屏：了解工具功能 + 看信任信息
   ↓ 设计重点：品牌标识、一句话说明价值、CTA 清晰
③ 逐题回答 6-7 个问题（进度条推进）
   ↓ 设计重点：一次一个问题、不压迫、不推销
④ AI 生成中（2-3 秒过渡动画）
   ↓ 设计重点：AI 感、不焦虑、预期管理
⑤ 结果页：展示预算/方案建议
   ↓ 设计重点：核心结论先行、模块清晰、CTA 自然
⑥ 点击"获取完整方案" → 留资弹窗
   ↓ 设计重点：不打断、价值在前留资在后
⑦ 提交成功 → 提示"顾问将在 XX 小时内联系你"
   ↓ 设计重点：明确下一步预期
```

### Journey B: 企业客户 AI 提需求流程
```
① 登录 Agent PC 端
② AI 顾问首页：看到输入区
   ↓ 设计重点：输入区是首屏主焦点，不是附属控件
③ 输入需求 → AI 首轮回应
   ↓ 设计重点：立即给反馈（识别了什么），不是空白等待
④ AI 追问（最多 3 轮）
   ↓ 设计重点：右侧面板实时更新识别进度
⑤ 需求补全 → AI 推荐 3 套方案
   ↓ 设计重点：方案对比清晰，从决策关键信息开始
⑥ 点击方案 → 查看详情 → 联系顾问
```

### Journey C: 销售跟进流程
```
① 登录 Supplier 工作台
② 顶部 KPI 总览（今日新增/待跟进/已报价/成交额）
   ↓ 设计重点：一眼掌握全局
③ 左侧商机队列：选择当前处理的商机
   ↓ 设计重点：快速切换、状态标签清晰
④ 中间详情：客户需求 + AI 识别 + 状态流转
   ↓ 设计重点：需求在前、识别在后、操作为辅
⑤ 右侧 AI 方案：查看/选择推荐方案
   ↓ 设计重点：辅助决策，不喧宾夺主
⑥ 底部：跟进时间线 / AI 话术 / 下一步动作（Tab 切换）
```

---

## 5. Information Architecture（信息架构）

### 5.1 全局导航体系

```
演立方 V4.7
├── 首页 (/)                          [所有人可见]
│
├── Agent 端 (/agent)                 [企业客户]
│   ├── AI 活动顾问 (/agent)           P0 — 首页 dashboard + 需求输入
│   ├── AI 对话 (/agent/assistant)     P0 — 对话式需求梳理
│   ├── 方案发现 (/agent/solutions)    P1 — 浏览/筛选方案库
│   ├── 消息中心 (/agent/messages)     P1 — 系统通知/报价/AI提醒
│   ├── 我的方案 (/agent/requests)     P1 — 历史需求列表
│   └── 报价详情 (/agent/quotations/$id) P1 — 单条报价查看
│
├── Supplier 端 (/supplier)           [销售/供应商]
│   ├── 销售作战台 (/supplier/workspace)    P0 — 总览 + 三栏工作区
│   ├── 商机中心 (/supplier/opportunities)  P0 — 商机列表+详情
│   ├── 线索中心 (/supplier/leads)          P0 — 线索列表+评分
│   ├── 方案管理 (/supplier/proposals)      P0 — 方案列表+编辑
│   ├── 报价管理 (/supplier/quotations)     P0 — 报价列表+版本管理
│   ├── 跟进中心 (/supplier/followups)      P1 — 跟进列表+时间线+AI话术
│   ├── 艺人管理 (/supplier/artists)        P1 — 艺人列表+详情
│   └── AI 反馈 (/supplier/feedback)        P2 — 反馈收集
│
├── Admin 端 (/admin)                 [平台运营]
│   ├── 平台大盘 (/admin/dashboard)    P0 — KPI + 趋势图 + 排行榜
│   ├── SKU 管理 (/admin/sku)          P0
│   ├── 艺人管理 (/admin/artists)      P0
│   ├── 企业客户 (/admin/customers)    P0
│   ├── 订单合同 (/admin/orders)       P1
│   ├── AI 反馈 (/admin/ai-feedback)   P1
│   ├── 标签系统 (/admin/labeling)     P1
│   ├── Prompt 管理 (/admin/prompts)   P1
│   ├── RBAC (/admin/rbac)             P1
│   └── 审计日志 (/admin/audit)        P1
│
├── 增长工具 H5 (/tools)              [公开]
│   ├── 预算计算器                     P0
│   ├── 保险方案生成器                 P0
│   └── 年会方案工具                   P1
│
├── 客户方案 H5 (/p/$proposalId)      [公开，链接分享]
│
└── m 端 (/m)                         [C 端消费者]
    ├── 首页                           P0
    ├── 发现                           P1
    ├── 消息                           P1
    ├── 提需求                         P0
    └── 我的                           P1
```

### 5.2 页面关系

- **增长工具 H5** → 留资 → **线索中心**（Supplier）
- **线索中心** → 创建商机 → **商机中心**（Supplier）
- **AI 顾问**（Agent） → 生成方案 → **方案管理**（Supplier） → **客户方案 H5**
- **报价管理** → 发送报价 → **消息中心**（Agent）

---

## 6. Navigation Design（导航设计）

### 6.1 Agent 端（桌面顶部导航 + 无侧栏）
```
┌─ [演立方 Logo] ──────────────────────────────────────────────────┐
│  AI活动顾问 │ 方案发现 │ AI对话 │ 我的方案 │ 消息中心 │ 🔔3 │ 👤王雅琳 │
└──────────────────────────────────────────────────────────────────┘
```
- 选中态：淡紫色背景高亮
- 消息红点：未读数量 badge

### 6.2 Supplier 端（桌面侧栏 + 顶部栏）
```
┌─ [Logo] ────────────────────────────── 🔔 │ 👤张运营 ──────────┐
│ 销售作战台  │ 商机中心 │ 线索中心 │ 方案管理 │ 艺人库 │ 跟进中心 │ ...
└──────────────────────────────────────────────────────────────────┘
```
- 顶部横向导航栏，下拉露出更多
- 选中态：紫色文字 + 底部指示条

### 6.3 Admin 端（桌面侧栏导航）
- 左侧垂直导航菜单（Ant Design Menu 组件）
- 分组：总览 | 内容管理 | 客户管理 | 系统管理

### 6.4 m 端（移动端底部 Tab）
```
┌──────────┬──────────┬──────────┬──────────┬──────────┐
│   首页   │  找方案  │  提需求  │  消息 3  │   我的   │
└──────────┴──────────┴──────────┴──────────┴──────────┘
```
- "提需求" 中间按钮稍大/突出
- 消息有未读红点

### 6.5 增长工具 H5 / 客户方案 H5
- **无导航**。单向漏斗式流程，无 Tab、无汉堡菜单、无全局页脚。

---

## 7. Screen Inventory（页面清单）

### Agent 端（企业客户 PC，7 页）

| # | 页面 | 路由 | P | 状态 | 设计动作 |
|:---:|:---|:---|:---:|:---:|:---|
| A1 | AI 活动顾问首页 | `/agent` | P0 | 已有 | 🔄 重设计 — v0 已出原型 |
| A2 | AI 对话 | `/agent/assistant` | P0 | 已有 | 🔄 重设计 — 左右分栏 |
| A3 | 方案发现 | `/agent/solutions` | P1 | 已有 | 🔄 重设计 — v0 已出原型 |
| A4 | 消息中心 | `/agent/messages` | P1 | 已有 | 🔄 重设计 — 列表层级优化 |
| A5 | 我的方案 | `/agent/requests` | P1 | 已有 | ⚡ 优化 — 卡片信息层级 |
| A6 | 报价详情 | `/agent/quotations/$id` | P1 | 已有 | ⚡ 优化 |
| A7 | 登录 | `/agent/login` | P0 | 已有 | 🔒 冻结 |

### Supplier 端（销售工作台 PC，14 页）

| # | 页面 | P | 状态 | 设计动作 |
|:---:|:---|:---:|:---:|:---|
| S1 | 销售作战台 | P0 | 已有 | 🔄 重设计 — 三栏层级收口 |
| S2 | 跟进中心 | P1 | 已有 | 🔄 重设计 — 时间线+AI建议 |
| S3 | 商机中心 | P0 | 已有 | ⚡ 优化 |
| S4 | 线索中心 | P0 | 已有 | 🔒 冻结 |
| S5 | 方案管理 | P0 | 已有 | ⚡ 优化 |
| S6 | 报价管理 | P0 | 已有 | ⚡ 优化 |
| S7-S14 | 其他 | P1-P2 | 已有 | ⚡ 优化 / 🔒 冻结 |

### Admin 端（16 页）、m 端（6 页）、增长工具 H5（3 页）、客户方案 H5（1 页）、首页（1 页）

> 完整清单省略，以上核心页面为重点。

---

## 8. Page Specification（关键页面详细规范）

### 8.1 A1 — AI 活动顾问首页

**页面目的**：客户进入后的第一屏。快速描述需求，浏览推荐方案。

**用户任务**：
1. 了解平台能做什么（0.5 秒扫读标题）
2. 输入/选择需求（2-5 秒）
3. 看到推荐结果（1-3 秒）

**页面结构**：
```
┌─ 标题 "今天想策划什么活动？"           ─┐
├─ AI 输入区（主焦点）                    ┤
│  输入框 + 示例 chips + 发送按钮          │
├─ [对话模式] 左右分栏                     ┤
│  左60%：AI 对话（消息气泡）              │
│  右40%：需求进度卡 + 推荐方案卡           │
├─ [浏览模式] 推荐方案卡 + 最近需求         ┤
└────────────────────────────────────────┘
```

**数据来源**：Mock 数据（后续接 AI API）
**状态**：默认态（有推荐）/ 对话中 / 加载中 / 空数据 / 错误

---

### 8.2 A3 — 方案发现

**页面目的**：通过筛选找到匹配的活动方案。

**页面结构**：
```
┌─ 标题 + 说明                           ─┐
├─ 筛选面板：搜索 + 类型/场景/人数/时长下拉  ┤
│  预算滑杆 ¥3万 – ¥20万                   │
├─ 结果区：3 列卡片网格                    ┤
│  每张卡片：类型/场景标签 + 标题 + 描述     │
│  人数/时长/地点 + 参考预算 + CTA          │
└────────────────────────────────────────┘
```

**筛选交互**：即时筛选（不点搜索按钮），防抖 300ms

---

### 8.3 A4 — 消息中心

**页面目的**：查看系统消息、报价通知、AI 提醒。

**页面结构**：
```
┌─ 🔔 消息中心 [3]        [全部标为已读]  ─┐
├─ 标签：全部(6) | 系统(1) | 方案(2) | ... ┤
├─ 消息列表                                ┤
│  类型标签(紫/蓝/灰/橙) + 标题 + 摘要 + 时间│
│  未读：红点 + 浅色背景                    │
└────────────────────────────────────────┘
```

---

### 8.4 S1 — 销售作战台

**页面目的**：销售主操作界面，管理商机→方案→报价全流程。

**关键设计问题**：三栏信息密度过高，需建立"列表→详情→决策"阅读路径。

```
┌─ KPI 总览（4 卡） ──────────────────────┐
├─ 左栏 25%：商机队列                     ┤
│  排序切换（推荐排序/最近更新）             │
│  商机卡片：公司名 + 状态 + 活动信息 + 优先级│
├─ 中栏 40%：当前商机详情                  ┤
│  ① 客户原始需求（首要）                   │
│  ② 结构化关键信息表                       │
│  ③ AI 需求识别（匹配度 + 已识别/缺失字段） │
│  ④ 状态流转                             │
├─ 右栏 35%：AI 方案生成（辅助）            ┤
│  方案卡片（紧凑） + AI 反馈入口            │
├─ 底部切换式：跟进时间线 | AI 话术 | 行动   ┤
└─────────────────────────────────────────┘
```

---

## 9. Interaction Rules（交互规范）

| 交互类型 | 规范 |
|:---|:---|
| **按钮** | 主按钮品牌紫(#5B4FD6)，次按钮白底灰边框，危险按钮红色 |
| **Loading** | 骨架屏（卡片级），不超过 2 秒的操作用 Spin |
| **Toast** | 右上角弹出，2.5 秒自动消失。成功绿色、失败红色、提示蓝色 |
| **Dialog** | 居中弹出，遮罩层半透明黑。确认/取消双按钮 |
| **Drawer** | 右侧滑出，宽度不超过 520px |
| **Table** | 奇偶行同色，悬停浅紫背景。分页在底部居中 |
| **Search** | 即时搜索（防抖 300ms），按 Enter 触发完整搜索 |
| **Filter** | 下拉选择即时生效，可多条件组合 |
| **Keyboard** | Enter 提交、Esc 关闭弹窗、Tab 切换焦点 |
| **Transition** | 页面切换 150ms fade，卡片出现 stagger 50ms |
| **Motion** | hover 微抬 2px + 阴影增强。AI 回复逐段淡入 |

---

## 10. Design System（设计系统）

> 完整 Token 定义见 `V4.7_Design_System_Final.md`。此处为 Trae Design 的速查版。

### 10.1 Color Tokens

```
品牌色
  --color-primary:           #5B4FD6   (按钮/链接/强调)
  --color-primary-hover:     #4A3FC5
  --color-primary-subtle:    #F0EEFF   (AI区域/选中态)

背景层级
  --color-bg-page:           #F7F8FA   (页面底层)
  --color-bg-surface:        #FFFFFF   (卡片/面板)
  --color-bg-ai:             #FAFAFF   (AI内容)

文字层级
  --color-text-primary:      #1A1D2E   (正文)
  --color-text-secondary:    #5B6178   (说明)
  --color-text-tertiary:     #8B92A8   (辅助/占位)

边框
  --color-border-default:    #E5E7EF
  --color-border-ai:         #E0DDFF

状态色
  Success: #00875A  Warning: #B45309  Error: #C53030  Info: #2563EB
```

### 10.2 Typography

```
Display:  36px/700 (首页标题)  24px/600 (页面大标题)
Heading:  18px/600 (卡片标题)  16px/600 (列表项)
Body:     16px/400 (描述)      14px/400 (正文)      13px/400 (紧凑)
Caption:  12px/400 (时间戳)    11px/400 (版本号)
```

### 10.3 Spacing & Radius

```
Spacing: 4px 基准 → 8 / 12 / 16 / 24 / 32 / 48
Radius:  4px(Tag)  8px(Button)  12px(Card)  24px(AI气泡)
Shadow:  sm(1px blur3) md(4px blur6) lg(10px blur15)
```

### 10.4 组件样式关键规则

- **主按钮**：48px 高（大）/ 40px（默认）/ 36px（紧凑），品牌紫背景，白字
- **次按钮**：透明背景 + #E5E7EF 边框
- **卡片**：白底 + 12px 圆角 + shadow-sm + #E5E7EF 边框
- **AI 区域**：#FAFAFF 背景 + #E0DDFF 边框 + 24px 圆角（对话气泡）
- **状态标签**：统一使用 StatusTag 组件，不得硬编码 `<Tag color="">`

---

## 11. Component Inventory（组件清单）

### 11.1 必须复用的现有组件（17 个）
`StatusTag` / `SolutionCard` / `OpportunityCard` / `FollowUpTimeline` / `AIChatPanel` / `AIFeedbackBar` / `RequirementExtractPanel` / `LeadCaptureModal` / `ToolQuestionFlow` / `ToolResultPage` / `DashboardCard` / `ProposalPreview` / `LeadScoreBadge` / `OpportunityStatusFlow` / `AppTopBar` / `MinimalRequirementForm` / `formatters(yuan/wan/relativeTime)`

### 11.2 建议 Trae Design 新增的设计规范组件
- 统一的消息列表项组件
- AI 对话气泡组件（含置信度标识）
- 筛选面板组件
- 卡片网格布局容器
- 空状态插画组件
- 页面级 Header（标题 + 说明 + 操作区）

---

## 12. Responsive Strategy（响应式策略）

| 端口 | 策略 | 断点 |
|:---|:---|:---|
| Agent PC | 桌面优先，最小 1024px | 768px 以下单列 |
| Supplier PC | 桌面优先，三栏变单列 | 1200/768px |
| Admin PC | 桌面优先，侧栏收为汉堡 | 1024px |
| 增长工具 H5 | 移动优先，375-480px 基准 | 最大宽度 480px 居中 |
| 客户方案 H5 | 移动优先 | 最大宽度 480px 居中 |
| m 端 | 移动优先 | 375-414px |
| 首页 | 桌面优先 | 768px 以下单列 |

---

## 13. Accessibility（无障碍）

- 颜色对比度 ≥ 4.5:1（正文）、≥ 3:1（大标题）
- 所有交互元素可键盘访问（Tab / Enter / Esc）
- 表单必填项有明确标识（文字 + 星号）
- 错误提示不使用仅颜色传达
- 按钮最小可点击区域 44×44px

---

## 14. Design Constraints（设计约束）

Trae Design **不得**：
- 改变用户角色或权限体系
- 修改业务规则（状态流转、报价计算、线索分配逻辑）
- 新增或删除路由
- 要求修改后端 API 或数据库
- 推翻现有品牌色 #5B4FD6
- 更换现有 UI 组件库（Ant Design 6）
- 引入第二套大型 UI 框架

Trae Design **应当**：
- 在现有技术栈和业务逻辑基础上，做视觉和体验层的提升
- 设计新的页面结构时保持与已有路由体系的兼容
- 优先优化信息架构和交互流程，其次才是视觉美化

---

## 15. Success Criteria（设计成功标准）

| 维度 | 标准 |
|:---|:---|
| **视觉一致性** | 所有 52 页使用统一的 Design Token，无硬编码视觉值 |
| **信息架构** | 每个页面 3 秒内可理解"我在哪、能干什么、下一步是什么" |
| **主次分明** | 每个页面有且只有一个主视觉焦点 |
| **状态完整** | 每个数据展示组件覆盖 Loading/Empty/Error/Default 四态 |
| **响应式** | 各端口在目标断点下不溢出、不横向滚动 |
| **组件复用率** | 共享组件复用率 ≥ 80% |
| **可交付** | 输出 Design Handoff 可直接下发给 Lovable/v0/Codex |
