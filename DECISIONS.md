# 演立方重大决策

> 只记录已经确认、会长期影响产品或工程方向的决定。普通开发过程写入任务卡或 Git，不写入本文件。

## 当前有效决策

### 2026-07-15 — Memory First Development Protocol

- **决定**：演立方所有 Codex 研发任务必须在修改前查询 OpenViking、验证原始事实源并生成 Memory Receipt；任务结束执行 Knowledge Impact Analysis。OpenViking 是项目长期记忆与发现层，仓库权威文件、代码、Schema、测试和 Git 继续负责定案。
- **原因**：项目开发周期长、PRD 持续升级且会有多个 Agent，不能依赖单个聊天窗口维持一致认知，也不能让检索摘要覆盖真实事实。
- **影响**：根 `AGENTS.md`、所有研发 Agent、任务启动/结束流程、Codex MCP、项目记忆脚本与 OpenViking 项目空间。
- **限制**：正式记忆必须可追溯；未批准调整只能是 `Proposed`；秘密和完整聊天/源码不得导入；高风险任务预检失败必须停止。
- **可逆性**：可以用新的 Approved 决策替换接入实现，但不得取消“跨窗口事实验证”底线。

### 2026-07-15 — 唯一工作空间与三条版本线

- **决定**：`/Users/wudixingyunxingleo/projects/演立方` 是唯一工作空间；产品规格、代码发布和设计基线独立管理。
- **原因**：V6.6 产品目标、V4.7 代码实现和 V4.7/V1.0 设计基线并不同步，必须避免虚假完成状态。
- **影响**：当前产品入口是 `docs/PRD.md`，代码状态以 Git/`PROJECT_STATUS.md` 为准。
- **可逆性**：可通过新决策调整，历史 PRD、Tag 和 Migration 不改写。

### 2026-07-15 — 平台、Tenant 与演员主体边界

- **决定**：演立方由 Hao Works / 演立方团队运营；后仰喜剧只是第一家普通 Tenant。演员使用平台身份，与一个或多个 Tenant 建立可生效、到期或终止的合作关系；独立演员同时拥有裁剪版独立演员 Tenant。
- **原因**：确保平台权责、Tenant 数据主权和演员跨 Tenant 合作边界清晰。
- **影响**：所有 V6.6 业务对象、权限和数据模型；旧 `company_id` 单归属模型不能作为最终实现。
- **可逆性**：产品核心定义，变更必须升级 PRD 并由项目负责人确认。

### 2026-07-15 — 产品业务 Agent 的 MVP 形态不变

- **决定**：演立方产品内 MVP 继续采用一个主 Agent + 确定性工具；报价由规则引擎计算。
- **原因**：本次设计对象是研发协作 Agent，不以工程管理为由提前拆分产品 Agent。
- **影响**：V6.6 §11 和后续 AI 功能实现。
- **可逆性**：只有新 PRD 明确调整后才可改变。

### 2026-07-15 — MVP 研发采用三个角色、默认串行

- **决定**：研发只设研发总调度、开发执行、独立审查三个角色；项目负责人只与总调度交互。默认一个开发 Agent，严格满足并行条件时最多两个开发实例。
- **原因**：个人 Vibe Coding 当前首要目标是持续推进、减少失控和返工，而不是最大化并行。
- **影响**：任务创建、分支、审查、状态更新和 Agent 提示词。
- **可逆性**：MVP 验证后可单独升级工程体系。

### 2026-07-15 — main 受保护，生产发布必须人工确认

- **决定**：所有任务使用短生命周期分支，快速修改也不直接进入 `main`；高风险任务必须独立审查和老板确认。生产不自动部署，正式发布必须由项目负责人确认并可回滚。
- **原因**：当前前端检查未全绿、自动测试缺失、发布路径尚不可靠。
- **影响**：Git、CI、发布和数据库操作。
- **可逆性**：仓库成熟且形成稳定门禁后可重新评估。

### 2026-07-10 — 设计基线

- **决定**：当前已落地品牌主色为 `#5B4FD6`，设计基线为 V4.7 / V1.0。
- **原因**：保持现有界面一致性；产品规格升级不自动触发全量视觉重做。
- **影响**：PC/H5、小程序和设计文档。
- **可逆性**：重大 Design System 变更需独立决策。

## 已被替代的历史决策

- 2026-07-10 的固定 Hermes/Codex/Qoder/Trae 多角色分工和“最多三个 Agent 并行”被本文件的三角色、默认串行规则替代。
- 以 `~/Desktop/agent-tasks/` 为唯一任务板的方式被仓库内 `templates/`、任务分支和 `PROJECT_STATUS.md` 替代。
- “OpenViking 只作为可选辅助、不是任务硬依赖”的旧规则已被 Memory First Development Protocol 替代；“仓库原始事实源优先于摘要”的部分继续有效。
- 旧决策全文仍可从 Git 历史追溯，不再维护第二份当前决策日志。

## 何时必须新增决策

仅限：核心产品规则或范围变化、核心架构/数据库策略变化、权限与安全策略变化、发布方式变化、核心依赖主版本替换、大规模重构、删除重要功能、研发 Agent 组织方式变化。
