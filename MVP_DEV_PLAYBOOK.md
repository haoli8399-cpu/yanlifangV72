# 演立方 MVP 轻量多 Agent 研发协作方案

## 1. 目标与边界

这套方案服务一个不懂软件开发的个人负责人。目标是：

```text
描述业务需求
→ 研发总调度定位 PRD、判断风险并建任务卡
→ 开发执行 Agent 在分支内实现和自测
→ 独立审查 Agent 验证
→ 总调度验收、合并并更新状态
→ 项目负责人只处理必要的老板决策和正式发布确认
```

本阶段不建设大型 DevOps、微服务治理、自动生产发布、自动合并高风险代码、大量常驻 Agent 或复杂研发指标平台。

## 2. 组织方式

### 研发总调度 Agent

项目负责人的唯一主要交互入口。负责 PRD 定位、范围判断、影响分析、任务卡、排序与并行判断、交付汇总、审查结论判断、状态更新和老板决策卡。原则上不承担大规模编码。

### 开发执行 Agent

默认一个实例。只按任务卡修改，执行必要检查，报告真实结果、未完成项和风险。满足第 5 节全部条件时，最多增加到两个实例。

### 独立审查 Agent

不参与被审任务的实现。审查真实差异、测试证据、权限/Tenant/金额/状态规则和回滚方式，输出“通过 / 修改后通过 / 驳回”。默认只读，不直接修代码。

三份可执行提示词位于 [`agent-prompts/`](agent-prompts/README.md)。

## 3. 三种任务类型

### 快速修改

适用：明确文案、小范围样式、无业务逻辑的轻微调整、低风险配置。

流程：总调度确认范围 → 开发修改 → 最低检查 → 总调度确认。

当前仓库即使是快速修改也使用任务分支，不直接改 `main`。一旦触及业务逻辑、数据、接口或权限，自动升级为标准或高风险任务。

### 标准任务

适用：普通页面、普通接口、独立组件、明确业务功能、普通 Bug。

流程：任务卡 → 开发 → 自测 → 独立审查 → 必要修复 → 验收 → 合并。

### 高风险任务

满足任一条件即为高风险：Schema/Migration、Tenant 隔离、认证权限、报价金额、合同收款、数据删除、核心状态机、生产环境、大规模重构、跨多个核心模块或改变 PRD 核心规则。

必须增加：影响分析、回滚方案、可复跑测试、独立审查、老板决策卡和合并/发布前人工确认。

## 4. 最小开发闭环

1. **需求输入**：项目负责人描述用户、场景、期望结果；无需给技术方案。
2. **Memory Preflight**：查询 OpenViking、验证权威来源和 Git，生成 Memory Receipt；失败时按风险停止或使用明确的新鲜只读快照。
3. **PRD 定位**：总调度绑定当前 PRD 版本、F 编号和章节；没有对应项则判断是否需要产品决策。
4. **范围与影响**：读取真实代码、当前状态和未提交修改，列出包含/不包含、预计文件和禁止范围。
5. **任务卡**：标准/高风险任务使用 [`templates/TASK.md`](templates/TASK.md)。
6. **分支实现**：开发 Agent 只在任务分支或 worktree 工作；修改前 Memory Gate 必须通过。
7. **开发自测**：逐条验收并保存命令、结果和人工走查证据。
8. **独立审查**：使用 [`templates/REVIEW.md`](templates/REVIEW.md)，不得由开发者自审代替。
9. **修复与复审**：发现项回到同一开发 Agent；“修改后通过”必须复审。
10. **产品验收**：总调度把技术证据翻译为用户结果；只有产品规则/范围或正式发布才找老板。
11. **知识影响与更新**：执行 Knowledge Impact Analysis，先更新权威文件，再同步有长期价值且已获必要批准的记忆。
12. **合并与状态**：检查通过后合并；更新 `PROJECT_STATUS.md`，重大决定才更新 `DECISIONS.md`。

Memory First 的事实源、检索、回执、故障和写回协议统一见 [`docs/memory/README.md`](docs/memory/README.md)。

## 5. 并行是例外

只有同时满足以下条件才允许两个开发 Agent 并行：

- 业务目标独立，接口契约已确定。
- 不修改同一文件、Schema、共享类型、认证权限、全局状态和核心状态机。
- 不修改同一用户流程的前后端核心逻辑。
- 每个任务有精确文件边界、明确合并顺序和独立验收。
- 使用独立分支或 worktree。
- 总调度判断收益明确高于冲突和返工风险。

Tenant 隔离、认证权限、核心 Schema、报价规则、合同/金额/收款、生产配置和首个纵向闭环禁止并行。

## 6. PRD 版本管理与升级

### 当前生效版本如何确定

- [`docs/PRD.md`](docs/PRD.md) 是当前入口。
- [`versions.json`](versions.json) 记录机器可读版本。
- 版本化完整文档保存在 `docs/PRD_Vx.y.md`，历史版本不覆盖。

### 新 PRD 如何切换

新文件出现后不得直接改代码。总调度先执行：

```text
读取新旧 PRD
→ 按功能编号生成差异
→ 检查当前任务和代码
→ 分类影响
→ 输出《PRD版本升级影响报告》
→ 项目负责人确认
→ 更新 docs/PRD.md、versions.json、CHANGELOG.md 和 PROJECT_STATUS.md
```

影响报告至少包括：新增、删除、业务规则、用户流程、数据结构、权限、UI、仅文案、无代码影响；并说明已完成代码返工、应暂停任务、后续任务、数据库影响、上线影响、立即/延后建议和老板决策项。

未经项目负责人确认，新 PRD 只是候选版本，不能自动切换开发基线，也不能大规模修改代码。

### 任务如何绑定 PRD

每张任务卡必须填写 PRD 版本、功能编号、对应章节、用户价值、目标结果和验收标准。代码、任务卡和 PRD 冲突时停止扩展，输出影响分析；不得用代码现状反向篡改产品定义。

## 7. 最低测试策略

目标是保护真实风险，不追求覆盖率数字。

| 任务范围 | 当前最低命令 | 额外要求 |
|:---|:---|:---|
| 仅文档/治理 | `node scripts/check-governance.mjs` | 检查链接、版本和孤立文档 |
| 后端 | `cd backend && npm run typecheck && npm run build` | 新/改接口必须有可复跑的成功、权限失败和边界样本 |
| PC/H5 | `cd codebase && npm run check:type && npm run check:build && npm run lint` | 核心页面人工走查；当前红色基线先修复 |
| 小程序 | `cd miniprogram && npm run build:mp-weixin` | 在微信开发者工具走查涉及页面 |
| 发布前 | `bash scripts/pre-commit-check.sh` | Preview 冒烟 + 回滚点确认 |

优先补测试：Tenant 越权、角色权限、Brief 版本/确认、报价规则、合同金额、收款状态、状态机、客户版/内部版隔离、演员任务授权和核心 API。AI 功能用固定样本做离线评估，并分别记录事实、推断和失败降级结果。

当前没有测试框架。高风险任务不得只靠构建；任务卡必须把新增的可复跑测试文件和命令列入范围。测试失败不得合并。

## 8. Git 与代码管理

- 使用 `main` + 短生命周期任务分支，不采用大型 Git Flow。
- 一个任务一条分支；分支名带 F 编号或工程任务编号。
- 开发 Agent 不直接修改 `main`，快速修改也不例外。
- 一次提交只表达一个可回滚目的；先检查真实 diff，再提交。
- 标准任务通过最低检查和独立审查后，可由总调度完成技术合并判断。
- 高风险任务必须项目负责人确认后才合并或发布。
- 并行时使用 worktree；合并顺序由总调度预先写入任务卡。
- 回退优先使用新 revert 提交或部署上一稳定 Tag，不改写历史。

本次协作体系落地不自动提交或推送；是否提交由当前任务授权决定。

## 9. 最低发布方式

目标流程：

```text
本地开发
→ 最低检查
→ 固定 Preview 环境验证
→ 项目负责人确认正式发布
→ 手动或半自动部署生产
→ 发布后冒烟
→ 更新状态与回滚点
```

当前 `backend/docker-compose.yml`、`deploy.sh` 和服务端口/健康路径不一致，Docker 也未运行，因此现有脚本不算可靠发布路径。先创建并验证一条手工发布路径；在此之前不得生产发布。

任何 Agent 都不得自主执行生产数据删除、不可逆 Migration、自动生产部署或未确认的回滚。每次发布必须记录 Tag/Commit、Migration、环境、检查结果和回滚点。现有 [`docs/RELEASE_CHECKLIST.md`](docs/RELEASE_CHECKLIST.md) 作为发布执行清单。

## 10. 项目事实源

只维护以下当前事实：

| 文件 | 用途 |
|:---|:---|
| [`AGENTS.md`](AGENTS.md) | 所有研发 Agent 的强制规则 |
| [`PROJECT_STATUS.md`](PROJECT_STATUS.md) | 当前基线、完成、进行、下一步、阻塞和风险 |
| [`DECISIONS.md`](DECISIONS.md) | 已确认的重大产品/技术决定 |
| [`MVP_DEV_PLAYBOOK.md`](MVP_DEV_PLAYBOOK.md) | 本协作流程、测试、Git、发布和 PRD 升级规则 |
| [`templates/TASK.md`](templates/TASK.md) | 标准/高风险任务卡 |
| [`templates/REVIEW.md`](templates/REVIEW.md) | 独立审查报告 |

旧 `docs/STATUS.md`、`docs/DECISION_LOG.md`、`docs/TASK_TEMPLATE.md` 和 `docs/REVIEW_CHECKLIST.md` 仅保留兼容入口，不重复维护内容。

## 11. 项目负责人如何下需求

项目负责人只需提供：

```text
我希望谁：
在什么场景：
完成什么结果：
为什么重要：
我如何判断做对了：
```

不知道 PRD 编号、技术模块、测试命令或风险等级都没关系；研发总调度负责补齐。若请求涉及产品范围、核心流程或业务规则变化，总调度必须生成老板决策卡，而不是要求项目负责人做技术选择。

## 12. 完成标准

研发体系有效的判断不是文档数量，而是：需求能被映射到 PRD；任务范围可控；开发结果有真实证据；独立审查能识别伪完成；失败可回滚；PRD 升级先分析再改代码；项目负责人只处理必要的业务和上线决策。
