# 演立方 Project Memory 现状审计报告

> 审计日期：2026-07-15；范围：项目事实源、Git、Agent 规则、Codex 接入和本机 OpenViking 0.4.6。审计完成后才开始实施。

## 审计结论

1. 产品、代码、设计和状态事实源已通过上一轮治理分离，但尚无跨窗口记忆协议。
2. 仓库已有 `AGENTS.md`、`PROJECT_STATUS.md`、`DECISIONS.md`、研发手册和任务模板，可抽取并合并；不需要另建大型 Engineering OS。
3. OpenViking 安装于 Miniconda，版本 `0.4.6`；服务为 localhost dev 模式，`/health`、`/ready` 与本地 Embedding shim 均实测通过。
4. `openviking status` 仍报告 3 条历史 Embedding 队列错误和检索健康退化；当前无 pending/in-progress，向量库和模型健康。故使用 `/ready` 作为进入门禁，完整状态作为退化告警。
5. 已验证 CLI、HTTP API、Streamable HTTP MCP `/mcp`、导入、语义检索、读取、覆盖更新、删除、URI 隔离、Tags/时间/context type/filter 能力。
6. Codex CLI 版本 `0.144.1` 支持 HTTP MCP，但审计时尚未登记 OpenViking；当前会话也没有 OpenViking MCP 工具。
7. OpenViking CLI 当前配置身份为 `openclaw/openclaw-main`；Codex MCP 未携带身份 Header 时在 dev 模式解析为 `default/default`。项目知识空间应放在 `viking://user/default/resources/yanlifang-project-memory`。
8. 审计时不存在演立方项目知识空间、预检、回执、写回治理、过期检测或敏感白名单。
9. 仓库当前分支为 `feat/workspace-version-governance`，HEAD 与本地 `origin/main` 同为 `78a659e`；已有 78 项暂存治理改动，本任务必须增量保留。
10. 项目代码主要仍为 V4.7，缺 V6.6 Tenant/演员关系地基，测试和发布路径也存在已记录缺口；这些事实只进入摘要，不在本任务修改业务代码。

## 资产结论

| 结论 | 内容 |
|:---|:---|
| Core Asset | 当前 PRD、状态、决策、Git、OpenViking 0.4.6 的 MCP/CLI/API |
| Extract & Merge | 现有 AGENTS、研发手册、审计、架构和三 Agent 规则 |
| Rebuild | Memory Preflight、Gate、Receipt、来源哈希、写回和过期治理 |
| Deprecate | “OpenViking 只是可选辅助”的旧规则；不删除历史，只标记被新决定替代 |

## 推荐与边界

采用 Codex HTTP MCP + 仓库内统一 Memory CLI + AGENTS 强制规则 + 覆盖式本地回执 + 六份白名单知识包。OpenViking负责检索，原始文件负责定案。

暂不建设复杂知识图谱、全仓/聊天导入、自动批准正式记忆、远程多用户服务、复杂 CI/CD 或生产自动化。

## 同步说明

按文档治理流程尝试在实施前封存本地改动并同步远端，但运行环境拒绝对 78 项未提交修改执行 stash/rebase。未绕过该安全拒绝；随后确认工作树未改变，且本地 HEAD 与已知 `origin/main` 一致。本任务在现有分支上继续增量修改，不提交、不推送。
